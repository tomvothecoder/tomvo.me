/**
 * kwesforms is not written in TypeScript, so the module needs to be defined manually.
 */
declare module "kwesforms" {
  import kwesforms from "kwesforms";

  const kwesforms;

  export default kwesforms;
}
